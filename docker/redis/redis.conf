# Redis 生产配置（最小可用示例）
# 说明：该文件作为占位，可与命令行参数组合使用。

appendonly yes
appendfsync everysec
maxmemory 1gb
maxmemory-policy allkeys-lru
save 900 1
save 300 10
save 60 10000
timeout 0
tcp-keepalive 300
databases 16

