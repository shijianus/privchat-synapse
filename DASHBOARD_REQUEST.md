```markdown
# Matrix Dashboard 项目需求文档

## 文档说明

**文档类型**: 产品需求文档 (PRD - Product Requirements Document)  
**版本**: 1.0.0  
**创建日期**: 2025-01-XX  
**最后更新**: 2025-01-XX  
**文档状态**: 最终版  
**目标读者**: 开发团队、UI/UX 设计师、测试工程师

---

## 1. 项目概述

### 1.1 项目背景

基于 Synapse Matrix 服务器，需要构建一个独立的管理 Dashboard 系统，用于内网 VPS 部署。该系统与 Synapse 协同工作但保持独立，不破坏 Synapse 的核心功能。

### 1.2 项目目标

- 提供完整的用户生命周期管理能力
- 实现细粒度的管理员权限控制系统
- 建立多级审核机制确保处罚决策的公正性
- 提供直观的数据可视化和系统监控能力
- 支持多语言和主题自定义
- 确保所有操作可追溯、可审计

### 1.3 部署环境

- **开发环境**: Windows
- **生产环境**: Ubuntu Server
- **访问范围**: 仅内网访问 (127.0.0.1)
- **部署位置**: Dashboard 与 Synapse 部署在同一台机器
- **端口分配**:
  - Synapse: 8008
  - Dashboard 前端: 3000
  - Dashboard 后端 API: 3000
  - 后端监控页面: 3001 (完全公开，无需认证)

### 1.4 核心原则

1. **不破坏 Synapse 核心功能**: 端到端加密、联邦通信、基础聊天功能必须完全保留
2. **最小化改动 Synapse**: 只在必要的检查点插入风控逻辑
3. **数据一致性优先**: 使用共享数据库和 Redis 缓存失效机制
4. **操作可追溯**: 所有管理操作必须记录完整的审计日志
5. **权力分离**: Dashboard 是管理者，Synapse 是执行者

---

## 2. 系统架构需求

### 2.1 整体架构

系统由三个独立但协同的部分组成：

1. **前端 Dashboard** (端口 3000)
   - 管理员登录认证
   - 可视化数据展示
   - 权限管理界面
   - 成员管理界面
   - 个性化设置

2. **Dashboard 后端 API** (端口 3000)
   - RESTful API
   - JWT 认证
   - 业务逻辑处理
   - 数据库操作
   - Redis Pub/Sub 事件发布

3. **后端监控页面** (端口 3001)
   - 完全公开访问（内网）
   - 只读展示
   - 实时系统状态
   - 服务器资源监控
   - 无需任何认证

### 2.2 数据存储

**PostgreSQL 共享数据库**:
- Synapse 使用 `public` schema
- Dashboard 使用 `dashboard` schema
- 通过外键关联用户身份

**Redis**:
- 用作缓存层
- Pub/Sub 消息总线
- 会话存储

**MinIO**:
- 对象存储
- 媒体文件存储

### 2.3 数据一致性策略

**缓存机制**:
- Synapse 缓存用户风控状态以提高性能
- 采用懒加载机制（用户首次操作时加载）
- 缓存设置 TTL 作为兜底机制

**缓存失效**:
- Dashboard 修改用户状态后立即删除 Redis 缓存
- 通过 Redis Pub/Sub 发布事件通知 Synapse
- Synapse 订阅事件并删除本地缓存
- 下次查询时自动从数据库获取最新状态

**缓存分层**:
- 高频数据（风控状态、设备列表）: 5-15分钟 TTL + Pub/Sub 快速失效
- 低频数据（用户组配置、个人偏好）: 30分钟 TTL + 仅 TTL 失效

---

## 3. 用户管理需求

### 3.1 用户注册管理

#### 3.1.1 注册方式配置

Dashboard 必须提供配置选项，管理员可以选择：
- 完全禁止自动注册
- 只允许邮箱验证注册
- 只允许手机号验证注册
- 同时允许邮箱和手机号注册

#### 3.1.2 注册信息收集

用户注册时必须提供：
1. **唯一用户名**: 不可重复
2. **邮箱地址**: 用于验证和联系
3. **手机号**: 必须与用户 IP 对应的国家匹配
4. **密码**: 至少 8 位
5. **确认密码**: 必须与密码一致
6. **人机验证**: 使用 Cloudflare Turnstile

手机号验证规则：
- 系统根据用户 IP 地址自动锁定其所在国家
- 只允许填写该国家的本地手机号
- 验证手机号格式是否符合该国家标准

#### 3.1.3 注册审核流程

**自动注册开启时**:
1. 用户提交注册信息
2. Dashboard 进行风控检查：
   - IP 纯净度分析
   - 黑名单比对（IP/邮箱/手机号/设备指纹）
   - 设备指纹检查
3. 通过检查后发送验证码（邮箱或手机号）
4. 用户输入验证码
5. 系统自动创建账户

**自动注册关闭时**:
1. 用户只能提交注册申请表
2. 无法获取验证码
3. 申请进入待审核队列
4. 管理员在 Dashboard 中查看并决定是否批准
5. 批准后可选择自动发送激活邮件
6. 拒绝后用户信息进入黑名单

#### 3.1.4 注册黑名单机制

**拒绝注册时的处理**:
- 用户名、邮箱、手机号、IP 地址、设备指纹全部进入黑名单
- 用户在客户端看到的提示: "账号还未被激活，请等待管理员激活您的账户"
- 不明确告知用户已被拒绝

**黑名单提示规则**:
- 相同用户名: "该用户名已经注册了一个账号"
- 相同邮箱/手机号: "该邮箱/手机号已经注册了一个账号"
- 相同 IP/设备指纹: 表面上显示"注册成功"，但在后续风控检测中直接封禁

**黑名单管理**:
- 管理员可以在 Dashboard 的垃圾箱中查看被拒绝的申请
- 管理员可以选择重新同意某个申请，将其移出黑名单并激活账户

### 3.2 用户组管理

#### 3.2.1 用户组分类

系统默认包含四个用户组（超级管理员可以创建更多）：
- 免费组
- 普通组
- 高级组
- 企业组

#### 3.2.2 存储配额

**文本存储**:
- 所有用户组: 必须支持文本存储（基础功能）

**非文本存储**:
- 免费组: 仅文本存储
- 普通组: 100MB
- 高级组: 1GB
- 企业组: 10GB（可配置为无限）

#### 3.2.3 消息速率限制

- 免费组: 10 条/分钟
- 普通组: 30 条/分钟
- 高级组: 100 条/分钟
- 企业组: 无限制

#### 3.2.4 AI 功能限制

所有用户组都可以使用 AI 功能，但使用次数不同：

- 免费组: OCR 10次/天, PDF转MD 10次/天
- 普通组: OCR 50次/天, PDF转MD 50次/天
- 高级组: OCR 200次/天, PDF转MD 200次/天
- 企业组: 无限制

#### 3.2.5 增值权限

用户组权限**不包括**基础功能（能否创建房间、邀请成员、上传文件、语音通话等），这些对所有用户免费开放。

用户组权限**包括**增值权限：
- 用户所属的用户组
- 存储的最大空间上限
- 是否对非文本内容进行存储
- AI 使用次数
- 能否创建高级加密房间（加密级别更高）
- 房间人数上限（默认 100 人，可扩展）
- 自定义表情包（可选）
- 创建 Bot（功能未实现时显示为灰色）
- 高级搜索（可选）

#### 3.2.6 用户组分配

- 用户组只能由管理员在 Dashboard 中手动分配或降级
- 用户无法自行升级或变更用户组

### 3.3 管理员直接创建用户（仅超级管理员）

- **权限与入口**: 仅超级管理员在 Dashboard「成员管理」中可见“快速创建用户”入口，所有创建操作必须写入审计日志并带上操作者信息。
- **创建方式**: Dashboard 后端调用服务器侧（Synapse 管理接口）的用户创建 API 来落库，与常规注册保持同样的唯一性、密码复杂度、黑名单校验和冷却期策略。
- **表单字段**: 用户名、显示昵称（可选）、初始密码或自动生成一次性密码、用户组选择、可选邮箱/手机号同步到 Profile、是否强制首次登录修改密码。
- **默认策略**: 创建时可勾选初始风控状态（正常/禁言/软封禁）、是否自动加入默认房间列表、是否向 Bot 发送欢迎或安全提示消息。
- **安全控制**: 调用接口需超级管理员 JWT + 权限校验，并要求通过二级密码/2FA 复核；失败需提供明确的校验原因（重名、黑名单等），成功返回 Matrix ID 与初始密码（如自动生成）。
- **前后端联动（新增要求）**: Dashboard 必须显式标注“调用服务器 API 创建用户”，实际通过内网 127.0.0.1 的 Synapse 管理接口完成创建，复用服务器端所有唯一性校验、黑名单、密码复杂度与冷却期策略，禁止在 Dashboard 侧跳过风控逻辑；创建入口与操作按钮仅对 Level 1 超级管理员可见。
- **创建流程与 UI 体验**:
  1. 超级管理员在“成员管理”页点击“快速创建用户”按钮，弹出分步抽屉/模态（顶部显示当前管理员、将创建的服务器实例）。
  2. 第一步填写账号信息（用户名、显示昵称、邮箱/手机号可选），实时校验重名、黑名单命中，并显示对应校验提示。
  3. 第二步设置登录策略（自定义密码或自动生成一次性密码、是否强制首次登录改密、所属用户组、初始风控状态），密码强度以颜色标签提示；可选勾选“创建后加入默认房间”“发送 Bot 欢迎/安全提示消息”。
  4. 第三步进行安全复核（二级密码/2FA + 二次确认弹窗），提示将调用服务器 API 写库并记录审计日志。
  5. 提交后显示加载进度与实时状态（调用服务器 API → 等待响应 → 写入审计日志），失败时展示可读错误（如黑名单命中/重名），成功时展示 Matrix ID、一次性密码/重置链接，提供一键复制与“下载 PDF/打印”选项。
  6. 完成页面提供“继续创建下一个”和“返回成员管理”双按钮，保持 Cloudflare 风格卡片、渐进式阴影与清晰的色彩层次，统一深浅色主题适配。
- **审计与安全留痕**: 每次创建的请求/响应、复核动作、选择的默认房间/风控状态/通知选项都写入 `operation_logs`，便于回溯；所有信息只在成功页短暂展示并可选择自动销毁一次性密码，以降低泄露风险。

---

## 4. 风控体系需求

### 4.1 风控级别定义

#### 4.1.1 禁言 (Silence)

**状态描述**:
- 用户可以正常登录
- 可以查看消息
- 可以使用 AI 等附加功能
- **不能**发送新消息

**时长**: 有明确期限，到期后自动解除

#### 4.1.2 软封禁 (Soft Ban)

**状态描述**:
- 用户可以登录
- 可以查看他人当前发送的消息
- **不能**发送消息
- **不能**使用任何附加功能
- 可以与管理员 Bot 交互进行申诉

**时长**: 可以是有期限的或永久的

#### 4.1.3 硬封禁 (Hard Ban)

**状态描述**:
- 用户尝试登录时收到"账号不存在"的提示
- 无法正常登录
- 如果用户已经登录，所有现有连接被立即强制断开
- 用户只能通过特殊方式与管理员 Bot 交互进行申诉
- 无法接收其他任何消息

**硬封禁期间的消息处理**:
- 其他用户发送给被硬封禁用户的消息被 Synapse 接收但不转发
- 这些消息存储在待发消息队列中
- 当用户解封后，这些消息流式同步给用户
- 不存在于服务器上的文件会被跳过

**时长**: 可以是有期限的或永久的

#### 4.1.4 软删除 (Soft Delete)

**状态描述**:
- 法律意义上的账户删除
- 数据实际上仍保留在数据库中
- 用户尝试登录时看到"账号不存在"
- 规则上不能恢复，除非有特殊情况

**冷却期**:
- 60 天冷却期
- 期间用户可以提取聊天记录和资料等数据
- 无法正常使用账户
- 无法通过管理员 Bot 申诉解封
- 冷却期结束后数据仍保留但标记为已删除

### 4.2 违规处罚体系

#### 4.2.1 一般违规

- 第一次: 禁言 8 小时
- 多次（2-4次）: 软封禁 7天/14天/1个月
- 很多次（4次以上）: 永久软封禁（接受申诉）

#### 4.2.2 普通违规

- 第一次: 禁言 24 小时
- 多次: 软封禁 2个月/4个月/1年
- 很多次: 永久硬封禁（接受邮件申诉）

#### 4.2.3 严重违规

- 第一次: 禁言 72 小时
- 多次（2-4次）: 硬封禁 8个月/2年/永久软封禁（接受申诉）
- 很多次: 2个月后删除账号（不接受申诉）

### 4.3 风控执行机制

所有风控操作由管理员在 Dashboard 中执行：

1. 管理员选择风控类型、时长、原因
2. Dashboard 修改数据库记录
3. Dashboard 删除 Redis 缓存
4. Dashboard 发布 Pub/Sub 事件
5. Synapse 接收事件后清除本地缓存
6. 如果是硬封禁，Synapse 调用内部 API 断开用户所有活动连接
7. 用户下次操作时，Synapse 从数据库查询最新状态并执行限制

**消息发送时的过滤**:
- Synapse 批量查询房间内所有成员的风控状态（使用 Redis MGET）
- 只向未被硬封禁的成员推送消息
- 被硬封禁的成员无法收到任何消息

### 4.4 严重度评分系统

#### 4.4.1 评分规则

处罚按照严重度进行评分，评分公式：

```
severity_score = base_score + (duration_hours × duration_multiplier)
```

**默认配置**（超级管理员可修改）:

| 操作类型 | 时长范围 | 基础分 | 时长乘数 | 示例 |
|---------|---------|--------|----------|------|
| 禁言 | 1小时 - 7天 | 10 | 0.1 | 禁言24小时 = 10 + (24×0.1) = 12.4分 |
| 软封禁 | 1天 - 1年 | 30 | 0.05 | 软封禁72小时 = 30 + (72×0.05) = 33.6分 |
| 硬封禁 | 7天 - 2年 | 60 | 0.03 | 硬封禁720小时 = 60 + (720×0.03) = 81.6分 |
| 软删除 | 60天起 | 100 | 0 | 固定100分 |

#### 4.4.2 差异判定

当两个管理员的处理结果评分差异超过**10分**（可配置），视为"差异过大"，需要仲裁。

---

## 5. 申诉系统需求

### 5.1 申诉渠道

#### 5.1.1 管理员 Bot

**Bot 特性**:
- Bot 是一个真实的 Matrix 用户账号
- 每个用户在注册时自动添加这个 Bot 为联系人
- 用户可以选择屏蔽 Bot，需要时可以重新唤出
- Bot 不具有特殊的聊天功能
- Bot 主要用于收集申诉信息并提交到 Dashboard
- Bot 通过 HTTP API 与 Dashboard 后端通信
- 使用 JWT Token 进行认证
- 所有通信必须使用端到端加密

**Bot 功能**:
1. 收集用户申诉信息
2. 查询 Dashboard API 获取处理结果
3. 将结果发送给用户
4. 协助 2FA 好友担保验证

#### 5.1.2 邮件申诉

对于永久硬封禁的用户，如果无法通过 Bot 申诉，可以通过发送邮件到指定邮箱进行申诉。

### 5.2 申诉信息收集

用户通过 Bot 提交申诉时，Bot 会询问：
1. **联系邮箱**: 用户填写（可以不是注册邮箱）
2. **申诉原因**: 用户阐述认为被误判的理由
3. **事情经过**: 用户详细描述使用账号的情况
4. **用户名**: Bot 自动收集
5. **是否知道被封原因**: Bot 询问以帮助用户理解违规行为

Bot 收集完信息后，将申诉数据提交到 Dashboard API，Dashboard 存储到数据库并通知管理员处理。

### 5.3 申诉处理流程

#### 5.3.1 基本流程

1. 用户通过 Bot 提交申诉
2. Dashboard 收到申诉并进入待处理队列
3. 管理员在 Dashboard 中查看申诉详情
4. 管理员做出处理决定（批准/拒绝/修改处罚）
5. 处理结果进入冷却期或立即生效
6. Bot 从 Dashboard API 获取处理结果
7. Bot 通过 Synapse 消息 API 将结果发送给用户

#### 5.3.2 冷却期机制

**冷却期规则**:
- 小管、管理员的处理有 **3 天冷却期**
- 超级管理员的处理**立即生效**
- 在冷却期内，其他同级或上级管理员可以修改处理结果
- 每次修改会重置冷却期

**示例流程**:
```
小管A处理申诉 → 3天冷却期开始
  ↓ (第1天)
管理员A修改处理 → 冷却期重置为3天
  ↓ (第1天)
管理员B修改处理（结果与A差异大） → 需要第3位管理员或超管介入
  ↓ (第2天)
管理员C处理 → 系统计算B和C的加权结果 → 2天后生效
  ↓ (第1天)
超级管理员处理 → 立即生效
```

#### 5.3.3 多级审核与加权计算

当多个同级管理员处理同一申诉且结果差异较大时：

1. **判定差异**: 评分差异超过10分
2. **需要仲裁**: 等待第3位管理员或超级管理员介入
3. **加权计算**: 系统根据管理员的准确率计算权重

**加权公式**:
```
weight[i] = accuracy_rate[i] / Σ(accuracy_rate)
final_score = Σ(score[i] × weight[i])
```

**示例**:
- 管理员A: score=30, accuracy=90% → weight=0.45
- 管理员B: score=40, accuracy=85% → weight=0.425
- 管理员C: score=35, accuracy=75% → weight=0.125
- final_score = 30×0.45 + 40×0.425 + 35×0.125 ≈ 34.875分

#### 5.3.4 准确率计算

每个管理员都有一个准确率评分：

**计算方式**:
1. 记录管理员每次处理的评分
2. 记录最终生效的评分（可能经过其他管理员修改）
3. 计算差异: `diff = |original_score - final_score|`
4. 累计差异: `total_diff += diff`
5. 计算平均差异: `avg_diff = total_diff / total_actions`
6. 计算准确率: `accuracy = MAX(0, 100 - (avg_diff / max_score × 100))`

准确率用于：
- 展示管理员的处理质量
- 作为加权计算的权重依据
- 帮助超级管理员评估管理员表现

### 5.4 申诉次数限制

**有期封禁**:
- 可以通过 Bot 申诉
- 申诉成功可以减少时间 3 次
- 或直接解封 2 次

**永久软封禁**:
- 可以通过 Bot 或邮件申诉
- 申诉成功可以转为有期封禁 2 次
- 或直接解封 1 次

**永久硬封禁**:
- 只能通过邮件申诉
- 申诉成功可以转为有期封禁 1 次

**注意**: 申诉转换后不能再次申诉同一处罚

---

## 6. 管理员权限系统需求

### 6.1 管理员分组

#### 6.1.1 默认分组

系统默认包含三个管理员分组：
1. **超级管理员** (Level 1): 拥有全部权限
2. **管理员** (Level 2): 管理下级和用户
3. **小管** (Level 3): 处理申诉和封禁

超级管理员可以在 Dashboard 中创建更多分组。

#### 6.1.2 管理员创建规则

**服务器创建**:
- 系统初始化时创建第一个超级管理员账户

**超级管理员创建**:
- 可以创建任何等级的管理员
- 可以设置每个管理员的权限上限
- 可以修改任何管理员的权限

**管理员创建下级**:
- 管理员可以创建小管
- 管理员可以设置小管的权限上限
- 但权限上限不能超过超级管理员设置的限制

**账户信息**:
- 创建时需要提供：账号、默认密码（管理员可以自行修改）、昵称
- 密码使用哈希存储
- 如果忘记密码，需要通过服务器或超级管理员重新设置
- 不通过邮箱恢复密码

#### 6.1.3 权限查看规则

- **超级管理员**: 可以查看所有管理员分组的信息
- **管理员**: 可以查看所有小管的信息
- **小管**: 不具有管理员分组信息面板

**可查看的信息包括**:
- 账号、昵称
- 状态（在线/离线多久）
- 处理过多少次申诉/注册信息
- 拥有的权限列表
- 发布过哪些任务
- 处理的正确率（有效次数）

### 6.2 权限体系

#### 6.2.1 权限设计理念

采用**混合方案**:
- 有预设角色（超管/管理员/小管）
- 上级管理员可以设置下级的权限上限
- 下级管理员可以在上限范围内自行调整
- 但不能超过上级设置的天花板

#### 6.2.2 基础配置权限（仅超级管理员）

以下权限只有超级管理员可见和修改，其他管理员**看不到**这个控制面板，只能看到最终的设置结果：

- 是否允许终端记录非文本内容
- 是否接入 AI 服务到客户端
- 接入哪些 AI 服务（OCR、PDF转MD 等）
- 黑名单被撤销后是否发送邮件提醒
- MinIO 连接配置
- Bot 基础设置
- 文件冷却期默认配置
- 注册模式配置（开启/关闭自动注册）
- 验证方式配置（邮箱/手机号）

#### 6.2.3 可分配权限

以下权限可以由上级管理员分配给下级：

**系统权限**:
- 管理管理员角色（创建新角色、修改角色权限）

**管理员管理**:
- 创建下级管理员
- 修改下级管理员权限
- 查看下级管理员列表
- 暂停下级管理员

**用户管理**:
- 审核注册
- 封禁用户
- 解封用户
- 修改用户组
- 添加黑名单
- 修改黑名单（放行）
- 删除黑名单（仅超管）

**申诉处理**:
- 处理申诉
- 查看申诉

**Bot 管理**:
- 发送 Bot 消息

**用户组配置**:
- 配置用户组的配额和权限

#### 6.2.4 权限上限机制

超级管理员可以为每个角色（如管理员、小管）设置权限上限：

**示例**:
- 超级管理员设置：管理员最多可以拥有"封禁用户"和"解封用户"权限
- 管理员A创建小管时，可以选择给予"封禁用户"权限，但不能给予"删除黑名单"权限（因为管理员自己也没有）

如果超级管理员没有特别设置，下级角色默认继承上级角色的所有权限。

### 6.3 管理员互相监督

#### 6.3.1 举报机制

**举报规则**:
- 同级管理员可以互相举报
- 下级管理员可以举报上级管理员
- 所有举报直接提交给超级管理员
- 支持匿名举报

**举报内容**:
- 举报理由（文本描述）
- 相关的处理记录
- 证据文件（可选上传）

#### 6.3.2 举报处理

**处理流程**:
1. 超级管理员收到举报
2. 查看举报详情和相关操作记录
3. 审查证据
4. 做出决定：
   - 驳回举报
   - 警告被举报人
   - 暂停被举报人的部分权限（可指定暂停天数）
   - 移除被举报人

**暂停权限**:
- 可以选择性暂停某些权限（如封禁用户、处理申诉）
- 设置暂停时长
- 是否通知被举报人
- 是否通知举报人结果

---

## 7. 黑名单系统需求

### 7.1 注册黑名单

#### 7.1.1 黑名单类型

- 用户名
- 邮箱地址
- 手机号
- IP 地址
- 设备指纹

#### 7.1.2 黑名单操作权限

**超级管理员**:
- 可以添加、修改、删除黑名单
- 操作立即生效

**管理员**:
- 可以添加黑名单
- 可以修改黑名单（放行）
- 不能删除黑名单
- 操作有 7 天冷却期

**小管**:
- 只能添加黑名单
- 操作有 7 天冷却期

#### 7.1.3 黑名单冷却期

与用户处罚类似，黑名单操作也有冷却期：
- 小管和管理员的操作: **7 天冷却期**
- 超级管理员的操作: **立即生效**
- 冷却期内其他管理员可以修改
- 每次修改重置冷却期

### 7.2 已注册用户黑名单

记录已注册用户的违规行为和处罚：

**记录内容**:
- 用户 ID
- 违规原因
- 处罚类型（禁言/软封禁/硬封禁/软删除）
- 处罚时长
- 到期时间

**权限规则**:
- **超级管理员**: 可以删除记录、修改处罚结果
- **管理员**: 可以修改处罚结果但不能删除记录
- **小管**: 只能查看

---

## 8. 二次身份验证 (2FA) 需求

### 8.1 2FA 验证方式

用户可以在以下验证方式中选择**其中一种**进行 2FA 验证（并联选择）：

1. **邮箱验证**: 发送验证码到邮箱
2. **TOTP 验证**: 使用身份验证器应用（如 Google Authenticator）
3. **安全码验证**: 使用预先生成的 10 个安全码中的任意一个
4. **好友担保验证**: 通过可信好友进行验证

**注意**: 手机号验证可能不可用（取决于系统配置）

### 8.2 二级密码

#### 8.2.1 二级密码用途

二级密码是专门用于以下场景的密码：
- 2FA 验证时使用
- Recovery Key 管理
- 修改邮箱、手机号等敏感信息

#### 8.2.2 二级密码设置

**首次设置**:
1. 必须通过邮箱或手机号验证
2. 必须与登录密码不同
3. 设置完成后必须至少配置一种 2FA 验证方式
4. 系统自动生成 10 个安全码

**修改/忘记**:
1. 先通过邮箱验证
2. 然后在 2FA 验证方式中选择 TOTP 或好友担保验证之一
3. 验证成功后才能重新设置

### 8.3 好友担保验证

#### 8.3.1 好友资格要求

可信好友必须满足以下条件：
- 好友账户已注册至少 **180 天**
- 与用户建立好友关系至少 **30 天**

Synapse 或 Dashboard 需要自动记录：
- 用户的注册时间
- 好友关系的建立时间

#### 8.3.2 验证流程

1. 用户 Alice 无法通过其他方式验证
2. 用户选择好友担保验证
3. 系统生成一个 **10 位随机哈希值**
4. 哈希值有效期 **48 小时**
5. Alice 通过其他方式将哈希值告知好友 Bob
6. Bob 在 Bot 中使用命令: `/verify alice a7b3c9d2e1`
7. Bot 验证好友资格和哈希值
8. 验证成功

#### 8.3.3 反悔机制

- 好友验证成功后有 **2 小时反悔期**
- 在反悔期内，好友可以撤销验证
- 反悔期内，用户的账户处于"不可信状态"
- 不可信状态: 可以登录但无法查看历史聊天记录和联系人（这是 Matrix 原有功能）

### 8.4 设备信任机制

#### 8.4.1 可信与不可信设备

**不可信设备**:
- 首次登录的设备默认为不可信
- 无法查看历史聊天记录
- 无法查看联系人列表
- 相当于一个新账户

**变为可信设备**:
- 通过二维码扫描验证
- 通过原设备验证
- 使用 Recovery Key 验证

#### 8.4.2 免 2FA 登录

- 用户可以选择在可信设备上 **180 天内免除 2FA 验证**
- 这个选项默认**关闭**
- 用户可以自行开启

### 8.5 Recovery Key 管理

#### 8.5.1 Recovery Key 用途

用于恢复：
- 加密的聊天记录
- 联系人列表

#### 8.5.2 管理规则

- Recovery Key 的管理必须使用**二级密码**而不是登录密码
- 只有在 Recovery Key 同步到 Synapse 服务器后，才能启用此功能
- 用户可以选择将 Recovery Key 加密后存储在服务器
- 也可以自行保管

#### 8.5.3 恢复方式

只能通过以下方式恢复聊天记录：
- 使用 Recovery Key
- 通过受信设备验证

---

## 9. 媒体存储管理需求

### 9.1 存储策略

#### 9.1.1 默认存储规则

- **文本消息**: 服务器强制存储
- **事件元数据**: 服务器强制存储
- **搜索索引**: 服务器强制存储
- **消息回执**: 服务器强制存储
- **缩略图**: 服务器存储（可配置）
- **图片/语音/视频/附件**: 客户端优先，服务器只保存元数据（可切换为服务器存储）
- **E2EE 密钥备份**: 只存储密文

#### 9.1.2 策略层级

存储策略支持三个层级，优先级从高到低：
1. **用户策略**: 针对单个用户
2. **房间策略**: 针对特定房间
3. **全局策略**: 默认策略

管理员可以在 Dashboard 中为不同范围配置不同的存储策略。

### 9.2 文件去重机制

**去重规则**:
1. 所有上传的文件计算 SHA256 哈希值
2. 上传前查询数据库是否已存在相同哈希值
3. 如果存在，直接引用已有文件的服务器 URL
4. 如果不存在，上传到 MinIO 对象存储

### 9.3 冷却期删除机制

#### 9.3.1 冷却期规则

- 文件上传到服务器后启动冷却期定时器
- 在冷却期内，如果有其他用户上传相同哈希值的文件，冷却期重置
- 冷却期结束后，如果文件没有被再次引用，从 MinIO 中删除

#### 9.3.2 不同用户组的冷却期

- 免费组: 7 天
- 普通组: 30 天
- 高级组: 90 天
- 企业组: 永久保留

### 9.4 端到端加密内容处理

#### 9.4.1 缩略图问题

- 服务器无法解密 E2EE 文件，无法自动生成缩略图
- 如果需要缩略图，客户端必须上传低分辨率的明文版本
- 限制: ≤320px, ≤50KB

#### 9.4.2 风险提示

- Dashboard 必须在配置此功能时显示风险提示
- 要求管理员勾选同意
- 所有涉及明文泄露的操作都必须弹窗确认并记录到审计日志

---

## 10. 后端监控页面需求

### 10.1 访问方式

- **URL**: `http://[内网IP]:3001/monitor`
- **权限**: 完全公开（内网），无需任何认证
- **目的**: 方便运维人员快速查看系统状态
- **特性**: 只读展示，无任何操作权限

### 10.2 显示内容

#### 10.2.1 系统资源监控

- **CPU 使用率**: 百分比 + 进度条，>80% 显示红色
- **内存使用**: 已用/总量 (GB) + 进度条，>90% 显示红色
- **磁盘使用**: 已用/总量 (GB) + 进度条
- **GPU 使用率**: 百分比 + 进度条（如果有 GPU）
- **网络流量**: 上行/下行速率 (MB/s)

#### 10.2.2 服务状态

检查以下服务是否运行：
- Synapse (端口 8008)
- Dashboard (端口 3000)
- PostgreSQL
- Redis
- MinIO (端口 9000)

显示状态:
- ✓ (绿色): 正常运行
- ✗ (红色): 服务异常
- ⚠ (黄色): 响应缓慢

#### 10.2.3 数据库连接池

- 总连接数
- 活跃连接数
- 空闲连接数
- 等待连接数

#### 10.2.4 Redis 缓存状态

- 已用内存
- 缓存命中率
- 键数量

#### 10.2.5 快速统计

- 在线用户数
- 今日消息数
- 活跃房间数
- 待处理申诉数

#### 10.2.6 最近错误日志

- 显示最近 1 小时的 WARN 和 ERROR 级别日志
- 显示时间戳、日志级别、消息内容

#### 10.2.7 系统信息

- 操作系统版本
- 系统运行时间
- Synapse 版本
- Dashboard 版本

### 10.3 刷新机制

- 页面每 **5 秒**自动刷新数据
- 显示"最后更新时间"

---

## 11. 前端 Dashboard 需求

### 11.1 登录界面

#### 11.1.1 界面元素

- Logo 和标题
- 用户名输入框
- 密码输入框
- "记住我"复选框
- 登录按钮
- "忘记密码？联系超级管理员"提示

#### 11.1.2 安全特性

- 登录失败 3 次后显示 Cloudflare Turnstile 验证码
- 登录失败 5 次后锁定账户 15 分钟
- 登录成功后显示上次登录时间和 IP

### 11.2 主仪表盘

#### 11.2.1 布局

- 顶部导航栏: Logo、通知图标、管理员名称下拉菜单、语言切换
- 左侧边栏: 导航菜单（概览、成员管理、申诉处理、管理员分组、系统配置、审计日志、登出）
- 主内容区: 根据选择的菜单显示对应内容

#### 11.2.2 概览页面内容

**关键指标卡片** (4个):
- 在线用户数（显示与昨日对比）
- 今日新增用户数（显示与昨日对比）
- 待处理申诉数（显示警告状态）
- 存储使用情况（显示百分比）

**数据可视化**:
1. **用户活跃度趋势**: 折线图，显示最近 7 天的活跃用户数
2. **用户组分布**: 饼状图，显示各用户组的人数和占比
3. **存储使用分析**: 堆叠条状图，显示视频、图片、文件、文本各占用的空间
4. **待处理事项**: 列表，显示需要在 48 小时内处理的申诉、待审核的注册申请、待处理的举报

### 11.3 成员管理界面

#### 11.3.1 三个 Tab

1. **注册申请管理**:
   - 显示所有注册申请列表（待审核、已批准、已拒绝）
   - 支持筛选和搜索
   - 点击详情查看 IP 分析、设备指纹分析
   - 批准或拒绝操作

2. **已注册用户**:
   - 不显示完整列表，只提供搜索框
   - 显示三个快捷筛选: 系统账户、待处理用户、已处理用户
   - 系统账户: 由管理员标记的测试账号
   - 待处理用户: 有申诉或操作待生效的用户
   - 已处理用户: 至少被一个管理员处理过的用户（无论是否在冷却期）
   - 搜索功能支持: 用户名、邮箱、用户ID、Matrix ID

3. **黑名单管理**:
   - 两个子 Tab: 注册黑名单、已注册用户黑名单
   - 注册黑名单: 显示被禁止注册的邮箱/IP/设备等
   - 已注册用户黑名单: 显示违规用户的处罚记录
   - 支持添加、修改（放行）、删除操作
   - 操作有 7 天冷却期（超管除外）

### 11.4 申诉处理界面

#### 11.4.1 申诉列表

- 显示所有申诉记录
- 支持筛选: 待处理、处理中、已解决
- 显示剩余处理时间（48 小时倒计时）
- 超时警告

#### 11.4.2 申诉详情

**显示内容**:
- 用户基本信息
- 当前处罚情况
- 执行人信息（包括准确率）
- 申诉内容（联系邮箱、申诉理由、事情经过）
- 用户历史处罚记录
- 其他管理员的处理意见（如果有）

**处理界面**:
- 选择处理方式: 保持原处罚 / 修改处罚 / 立即解封
- 如果修改处罚: 选择新的处罚类型和时长
- 填写处理理由
- 显示严重度评分预览
- 显示预计生效时间
- 显示提示信息（冷却期、通知等）

**冷却期可视化**:
- 如果有多个管理员处理，显示时间线
- 显示每次处理的评分和状态
- 显示是否需要仲裁
- 显示加权计算说明

### 11.5 管理员分组界面

**注意**: 此界面只有上级管理员可见。超级管理员可以看到所有管理员，管理员可以看到所有小管，小管不可见此界面。

#### 11.5.1 三个 Tab

1. **管理员列表**:
   - 显示所有下级管理员
   - 显示: 用户名、昵称、角色、状态、准确率、处理数量
   - 点击管理进入详情

2. **角色配置** (仅超级管理员):
   - 显示所有角色
   - 可以创建新角色
   - 编辑角色权限
   - 设置角色的默认权限

3. **举报记录** (仅超级管理员):
   - 显示所有举报
   - 处理举报: 驳回/警告/暂停权限/删除账户

#### 11.5.2 管理员详情

**四个子 Tab**:
1. **基本信息**: 用户名、昵称、角色、创建者、创建时间、最后登录
2. **权限配置**: 显示所有权限复选框，可以修改
3. **操作记录**: 显示该管理员的所有操作历史
4. **统计数据**: 总操作次数、准确率、平均响应时间等

**操作按钮**:
- 重置密码
- 暂停权限
- 删除账户

### 11.6 系统配置界面

**注意**: 此界面仅超级管理员可见。

#### 11.6.1 四个 Tab

1. **基础配置**:
   - 注册设置: 注册模式、验证方式、人机验证
   - 存储设置: 是否允许非文本存储、文件冷却期、MinIO 连接
   - AI 服务设置: 接入哪些 AI 服务
   - 黑名单设置: 是否发送邮件提醒、冷却期
   - Bot 设置: Bot 用户名、是否自动添加

2. **用户组配置**:
   - 显示所有用户组
   - 编辑用户组: 存储配额、消息速率、AI 额度、增值权限

3. **严重度配置**:
   - 显示严重度评分配置表
   - 差异判定阈值
   - 准确率计算公式说明
   - 加权计算公式说明

4. **帮助文本管理**:
   - 显示所有帮助文本
   - 编辑帮助文本: 简短文本（多语言）、悬停提示（多语言）、文档链接

### 11.7 个人设置界面

#### 11.7.1 四个 Tab

1. **账户信息**:
   - 显示用户名、昵称、角色、加入时间、最后登录
   - 修改密码按钮
   - 更新信息按钮

2. **外观设置**:
   - 语言选择下拉菜单（支持: 简体中文、English、繁體中文、日本語、한국어、Esperanto等）
   - 主题模式选择: 深色/浅色/自动
   - 自定义颜色:
     - 主品牌色选择器（带预设颜色）
     - 强调色选择器（带预设颜色）
     - 实时预览效果
   - 显示设置: 显示动画、显示辅助提示、紧凑模式、显示通知气泡

3. **通知设置**:
   - 邮件通知开关
   - 推送通知开关
   - 时区选择

4. **安全设置**:
   - 显示当前会话列表
   - 登出其他设备按钮

### 11.8 审计日志界面

- 显示所有管理员操作的历史记录
- 支持按时间、操作者、操作类型、目标用户筛选
- 涉及隐私风险的操作特别标注
- 支持导出为 CSV

---

## 12. UI/UX 设计需求

### 12.1 设计风格

**参考对象**: Cloudflare Dashboard

**核心特征**:
- 简洁、现代、专业
- 深色模式友好
- 清晰的信息层级
- 大量使用卡片布局
- 流畅的过渡动画

### 12.2 色彩方案

#### 12.2.1 默认深色主题

- **主背景**: #1a1a1a
- **卡片背景**: #242424
- **边框**: #3a3a3a
- **主文本**: #ffffff
- **次要文本**: #a0a0a0
- **主品牌色**: #3b82f6 (蓝色，可自定义)
- **强调色**: #f59e0b (橙色，可自定义)
- **成功**: #10b981 (绿色)
- **警告**: #f59e0b (黄色)
- **危险**: #ef4444 (红色)
- **信息**: #3b82f6 (蓝色)

#### 12.2.2 可选浅色主题

- **主背景**: #f9fafb
- **卡片背景**: #ffffff
- **边框**: #e5e7eb
- **主文本**: #111827
- **次要文本**: #6b7280

### 12.3 辅助提示系统

**悬停问号设计**:
- 在专业词汇旁边显示一个问号图标
- 鼠标悬停时显示简短解释（最多 2 行）
- 解释文本中可以包含超链接
- 点击超链接跳转到内部文档（内网的 Markdown 文档，不是外部网站）
- 所有解释文本支持多语言
- 只有超级管理员可以修改解释文本

### 12.4 响应式设计

- 支持桌面端（主要）
- 支持平板端（基本可用）
- 移动端低优先级（侧边栏折叠为汉堡菜单）

### 12.5 动画和过渡

- 页面切换: 淡入淡出（300ms）
- 卡片悬停: 轻微上浮 + 阴影增强
- 按钮点击: 轻微缩放
- 加载状态: 骨架屏或加载动画
- 所有动画可以在个人设置中关闭

---

## 13. 多语言系统需求

### 13.1 支持的语言

**必须支持**:
- 简体中文 (zh-CN)
- English (en)
- 繁體中文 (zh-TW)
- 日本語 (ja)
- 한국어 (ko)

**可选支持**:
- Esperanto (eo)
- Français (fr)
- Deutsch (de)
- Español (es)
- 其他语言...

### 13.2 语言切换

- 每个管理员可以独立选择界面语言
- 语言偏好保存在数据库中
- 每次登录自动加载用户偏好的语言

### 13.3 翻译范围

需要翻译的内容：
- 所有 UI 界面文本
- 按钮标签
- 表单字段
- 错误消息
- 提示信息
- 帮助文本
- 文档内容

不需要翻译的内容：
- 用户提交的申诉内容
- 管理员填写的处理理由
- 审计日志中的具体操作描述
- 用户名、邮箱等用户数据

---

## 14. 技术栈需求

### 14.1 后端技术栈

- **语言**: Node.js 20+ (TypeScript)
- **框架**: Express.js 4.x
- **数据库**: PostgreSQL 15+
- **缓存**: Redis 7+
- **对象存储**: MinIO
- **邮件服务**: Brevo API
- **人机验证**: Cloudflare Turnstile
- **认证**: JWT
- **日志**: Winston

### 14.2 前端技术栈

- **框架**: React 18+ (TypeScript)
- **构建工具**: Vite 5
- **UI 库**: shadcn/ui + Tailwind CSS
- **状态管理**: TanStack Query (React Query)
- **路由**: React Router 6
- **图表**: Recharts 或 Chart.js
- **图标**: Lucide React

### 14.3 Synapse 修改

- **语言**: Python 3.11+
- **框架**: Twisted (Synapse 原有)
- **Redis 客户端**: redis-py
- **数据库**: psycopg2

### 14.4 基础设施

- **容器化**: Docker + Docker Compose（开发环境）
- **反向代理**: Nginx（生产环境）
- **版本控制**: Git
- **CI/CD**: GitHub Actions（可选）

---

## 15. 安全需求

### 15.1 认证与授权

- 所有 API 请求必须通过 JWT Token 认证
- Token 过期时间: 24 小时
- 刷新 Token 机制
- 密码使用 bcrypt 加密（工作因子 ≥12）
- 二级密码同样使用 bcrypt 加密

### 15.2 权限验证

- 每个 API 端点必须验证用户权限
- 使用中间件统一处理权限检查
- 权限不足返回 403 Forbidden

### 15.3 输入验证

- 所有用户输入必须验证
- 使用验证库（如 Zod）定义输入 Schema
- 拒绝不符合格式的输入
- 防止 SQL 注入（使用参数化查询）
- 防止 XSS（React 默认转义，特殊情况使用 DOMPurify）

### 15.4 CSRF 防护

- 如果使用 Cookie 认证，必须验证 CSRF Token
- 如果使用 JWT Bearer Token，无需 CSRF Token

### 15.5 速率限制

- 登录接口: 5 次/15分钟
- 普通 API: 100 次/15分钟
- 使用 express-rate-limit

### 15.6 审计日志

- 所有管理操作必须记录到 operation_logs 表
- 记录内容: 操作者、操作类型、目标用户、详情、IP 地址、时间戳
- 敏感操作必须弹窗二次确认

---

## 16. 性能需求

### 16.1 响应时间

- API 响应时间 P95 < 200ms
- 页面加载时间 < 2s
- 数据库查询时间 < 100ms

### 16.2 并发支持

- 支持至少 100 个管理员同时在线
- 支持 10,000+ 注册用户

### 16.3 缓存策略

- 使用 Redis 缓存高频查询
- 缓存命中率 > 95%
- 使用 Redis MGET 批量查询

### 16.4 数据库优化

- 所有外键列创建索引
- 频繁查询的列创建索引
- 复合查询创建复合索引
- 分页查询必须使用 LIMIT 和 OFFSET

---

## 17. 测试需求

### 17.1 单元测试

- 核心业务逻辑覆盖率 ≥ 80%
- API 路由覆盖率 ≥ 70%
- 工具函数覆盖率 ≥ 90%

### 17.2 集成测试

- 测试完整的 API 端到端流程
- 使用真实的测试数据库（自动创建和清理）

### 17.3 E2E 测试

- 使用 Playwright 测试前端交互
- 关键流程必须有 E2E 覆盖: 注册、登录、封禁、申诉

---

## 18. 文档需求

### 18.1 必需文档

- **README.md**: 项目概述、快速开始
- **ARCHITECTURE.md**: 系统架构设计
- **API.md**: 完整 API 端点文档
- **DATABASE_SCHEMA.md**: 数据库表结构说明
- **DEPLOYMENT.md**: 部署指南
- **CHANGELOG.md**: 版本变更记录

### 18.2 内部文档系统

创建一个简单的 Markdown 文档系统：
- 支持多语言
- 文档路由: `/docs/:lang/:page`
- 从帮助文本链接到文档

### 18.3 代码文档

- 所有公共 API 和函数必须有 JSDoc/Docstring
- 复杂业务逻辑必须有注释
- 所有修改 Synapse 的地方必须添加 `# DASHBOARD INTEGRATION` 标记

---

## 19. 部署需求

### 19.1 开发环境

- 使用 Docker Compose 快速搭建
- 包含 PostgreSQL、Redis、MinIO
- 支持热重载

### 19.2 生产环境

- 使用 Nginx 反向代理
- 限制内网访问
- HTTPS 支持（可选）
- 日志轮转
- 自动备份数据库

---

## 20. 验收标准

### 20.1 功能完整性

- ✅ 所有需求中描述的功能都已实现
- ✅ 三层管理员权限系统正常工作
- ✅ 多级审核和冷却期机制正常工作
- ✅ 严重度评分系统正确计算
- ✅ 准确率跟踪正确更新
- ✅ 黑名单管理正常工作
- ✅ 申诉流程完整可用
-
